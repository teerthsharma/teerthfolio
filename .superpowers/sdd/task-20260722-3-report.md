# Task 3 — Cinematic Release Proof

Date: 2026-07-22
Commit: this report ships with `fix: finish cinematic release proof` (exact SHA recorded in the controller handoff)

## Outcome

Local implementation and release proof are complete. The live application passes the full 20-viewport cinematic verifier, the unscoped seven-station medium mechanism tour, a focused low-tier Manifold proof, all particle checks, lint, and the optimized Next.js build. The observatory dome now has bounded departure visibility and no suspense fallback silhouette can remain in world space.

No deployment, push, branch change, or other remote mutation was performed.

## RED evidence and root causes

- Low-tier Manifold initially failed the unchanged 5% hero near-black limit at `0.0595238095`.
- Contrast and reduced-motion cinematic variants initially supplied only `24/24` dark samples, below the unchanged minimum of `30`.
- The departure capture exposed a flat sphere/torus silhouette. It matched the scene suspense fallback geometry, not the observatory model.
- The unscoped station verifier timed out because the app auto-selected `high` while the verifier assumed its requested `medium` tier was already active.
- Once that wait was repaired, the unscoped proof exposed genuinely unreadable detailed materials in Manifold, Field, Upstream, and Topology rather than selector-only failures.
- Static particle/rescue checks contained exact-shape fixtures that had not yet admitted the added observatory diagnostics and shadow-separation budget field.

## Implementation

- Added an explicit observatory departure-distance policy, canvas diagnostics, a verifier screenshot/assertion after outbound S2 movement, and a `null` scene suspense fallback.
- Repaired the cinematic verifier for the current port, route movement semantics, HUD/archive selectors, safe-gate copy, responsive layout, and source evidence.
- Added a bounded low-tier paper shadow toe with highlight compensation. The global dark-pixel and brightness thresholds were not weakened.
- Lifted only the affected station palettes/material emissive floors so authored blue, graphite/copper, coral, and magenta structures remain readable after grading.
- Made the station verifier compare the canvas's actual quality with its requested tier and updated the current topology portal labels.
- Updated static contracts to cover the new post-process and diagnostics fields.

## GREEN runtime evidence

Final cinematic report: `verification/screenshots-cinematic-render/report.json`

| View | Average luminance | Dark pixels | Tonal range |
| --- | ---: | ---: | ---: |
| desktop | 163.07 | 152 | 219.01 |
| field docking (low) | 165.36 | 71 | 222.44 |
| iPad portrait (low) | 143.82 | 86 | 230.93 |
| iPad landscape (low) | 161.29 | 56 | 213.81 |
| mobile (low) | 147.26 | 74 | 232.03 |
| contrast (low) | 165.98 | 44 | 225.46 |
| reduced motion (low) | 166.67 | 43 | 226.81 |

All seven rows above have zero verifier failures and zero browser errors. The departure proof is `verification/screenshots-cinematic-render/observatory-departure-s2.png`; the canvas reports `data-observatory-dome-visible="false"` beyond the bounded home field, and visual inspection shows S2 intact without the previous fallback silhouette.

Final unscoped medium report: `.verification/station-mechanisms-live-medium/report.json`

| Station | Average luminance | Overall black | Hero black | Draw/program/texture |
| --- | ---: | ---: | ---: | --- |
| S2 Kernel Core | 162.72 | 0.035% | 0% | 12/3/0 |
| Manifold Reactor | 97.37 | 0.035% | 0% | 12/3/0 |
| Field Chamber Coils | 163.98 | 0.017% | 0% | 12/3/0 |
| QPU Ice Bridge | 171.72 | 0.035% | 0% | 12/3/0 |
| Upstream Radio Mast | 143.40 | 0% | 0% | 9/3/0 |
| Topology Archive Wall | 115.08 | 0.017% | 0% | 9/3/0 |
| Assembly Tool Locker | 74.22 | 0.035% | 0% | 9/3/0 |

The focused low-tier Manifold report is `.verification/station-mechanisms-live-task3-manifold-green-v5/report.json`: average `78.10`, overall black `0%`, hero black `2.656%` (limit `5%`), range `219.51`, budget `4/2/0`, zero failed requests, and zero browser errors. GPU `ReadPixels` stall notices are Chromium performance warnings generated by proof capture, not fatal diagnostics.

Particle runtime evidence: `.verification/wave-f-particles/report.json` with all eight stations captured.

## Final gates

- `npm run lint` — pass
- `npm run build` — pass, including every chained contract and Chromium biome shader compilation
- `npm run check:render-budget` — pass, 58 checks
- `npm run check:particles` — pass, `512/1536/4096`, one shared draw
- `npm run verify:particles` — pass, eight stations
- `npm run verify:station-mechanisms` — pass, seven stations unscoped at medium
- focused low Manifold verifier — pass
- `npm run verify:render` — pass, 20 viewports
- `git diff --check` — pass

## Read-only publication audit

- Vercel CLI identity: `teerthsharma` in scope `teerthsharmas-projects`.
- No local `.vercel/project.json` link is present. The read-only project listing did not show a linked `teerthfolio` project in its returned page, so no deploy target was inferred.
- GitHub CLI is authenticated as `teerthsharma`.
- Repository: `Debyte404/teerthfolio`; default branch: `main`; origin: `https://github.com/Debyte404/teerthfolio.git`.
- Working branch during proof: `codex/open-world-loading-navigation`.
- The Node development server remains live at `http://127.0.0.1:3000`.
