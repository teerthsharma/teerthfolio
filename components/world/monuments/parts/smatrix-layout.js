// Orphaned (round 3): Smatrix.jsx no longer imports anything from this file.
// It used to lay out the "smatrix" figure's bead spiral (K = 22 hops, the
// blue -> violet -> coral ramp) for a glass-case display inside the resolvent
// building; that whole system was removed as a redraw of the landing site's
// own figure, which a lab building must not show (see components/world/
// monuments/Smatrix.jsx's file header for the fix). Nothing in this file is
// referenced anywhere else in the codebase -- safe to delete.
